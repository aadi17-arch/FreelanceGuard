# 🚀 FreelanceGuard: Final Launch Checklist (Tomorrow's Tasks)

This document contains the **final 9 remaining tasks** required to make FreelanceGuard 100% production-ready. 

Everything else in the system (Database, Escrow Locking, Proposal Bidding, Authentication, JWT Security, Project Generation) is **fully operational and heavily tested**. Once you complete these final cleanup items, your platform is ready for real users.

---

## 🧠 LOGICAL UPDATES (Backend Data & Routing)

- [ ] **1. Strip the "Premium Demo" Mock Logic**
  - **Location:** `client/src/pages/contracts/Contracts.jsx`
  - **Task:** Remove the `showDemo` state, the toggle button, and the hardcoded arrays (`activeContracts` and `completedContracts`). Ensure the page relies strictly on the `axios.get("/escrow")` payload.

- [ ] **2. Build the Profile Activity Feed API**
  - **Location:** `client/src/pages/profile/profile.jsx` & `server/src/modules/auth/`
  - **Task:** The "Recent Activity" sidebar is currently hardcoded text. Create a backend route (`GET /api/auth/activity`) that fetches the user's latest 5 actions (e.g., bids submitted, milestones released) and maps them to the sidebar.

- [ ] **3. Implement Background Polling for Contracts**
  - **Location:** `client/src/pages/contracts/Contracts.jsx` & `client/src/pages/escrow/EscrowDashboard.jsx`
  - **Task:** Add a `setInterval` hook (running every 10 seconds) to quietly re-fetch `/escrow`. This ensures users see milestone updates in real-time without having to manually refresh the browser page.

- [ ] **4. Enable Evidence File Downloads**
  - **Location:** `client/src/pages/disputes/DisputeDetails.jsx`
  - **Task:** In the evidence list, wrap the file names in an HTML anchor tag: `<a href={file.fileUrl} download>` so clients and arbiters can actually download the uploaded JPGs/PDFs.

---

## 🎨 UI UPDATES (Visuals & Placeholders)

- [ ] **5. Dynamic Dispute Timeline Binding**
  - **Location:** `client/src/pages/disputes/DisputeDetails.jsx`
  - **Task:** The top progress bar (Reported ➔ Evidence ➔ Review ➔ Resolved) is hardcoded to look active. Bind the `active` and `done` properties to read the actual database `dispute.status`.

- [ ] **6. Replace the Fake "Live Chat" Toast**
  - **Location:** `client/src/pages/disputes/DisputesList.jsx`
  - **Task:** Clicking the "Live chat" button currently pops up a fake loading toast that always says "Agents are busy". Remove this fake logic and replace it with a `mailto:support@freelanceguard.com` action.

- [ ] **7. Connect Dead Support Hub Links**
  - **Location:** `client/src/pages/disputes/DisputesList.jsx`
  - **Task:** The "Email us" and "Help docs" buttons at the very bottom have no click handlers. Hook them up to trigger an email or route to a Terms/Privacy page.

- [ ] **8. Hide or Build "Coming Soon" Routes**
  - **Location:** `client/src/App.jsx` (Routes `/analytics` and `/messages`)
  - **Task:** These pages currently just show a blank "Coming Soon" screen. You must either hide the buttons from the navigation bar for Launch V1 or build out the actual messaging features.

- [ ] **9. Wire Up Dashboard Chart Analytics**
  - **Location:** `client/src/pages/dashboard/Dashboard.jsx`
  - **Task:** The circular charts and graphs are currently rendering static SVG arrays. They need to be updated to consume the real `totalSpent` and `totalEarned` numbers returned by the `/api/auth/profile` endpoint.

---
*Generated by Antigravity AI.*
